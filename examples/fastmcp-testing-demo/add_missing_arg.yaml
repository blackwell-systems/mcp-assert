# Add tool: calling with missing required argument should return an error
name: add with missing argument returns error
server:
  command: uvx
  args: [fastmcp, run, /tmp/fastmcp/examples/testing_demo/server.py]
assert:
  tool: add
  args:
    a: 5
  expect:
    is_error: true
timeout: 30s
