name: generate_liquid_chart returns expected result
server:
  command: npx
  args:
  - -y
  - '@antv/mcp-server-chart'
assert:
  tool: generate_liquid_chart
  args:
    percent: 1
  expect:
    not_error: true
timeout: 30s
