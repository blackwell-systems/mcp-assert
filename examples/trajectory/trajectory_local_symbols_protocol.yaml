# Trajectory assertion: validates the /lsp-local-symbols skill protocol.
# open_document must precede get_document_symbols (file must be loaded).
# get_document_symbols must precede get_document_highlights (need position from symbols).
# get_info_on_location is optional — called for type info at a position.
# Read-only — apply_edit and rename_symbol must NOT appear.
name: lsp-local-symbols follows skill protocol
trace:
  - tool: open_document
    args:
      file_path: "fixtures/go/main.go"
      language_id: "go"
  - tool: get_document_symbols
    args:
      file_path: "fixtures/go/main.go"
  - tool: get_document_highlights
    args:
      file_path: "fixtures/go/main.go"
      line: 6
      column: 6
  - tool: get_info_on_location
    args:
      file_path: "fixtures/go/main.go"
      line: 6
      column: 6
trajectory:
  - type: order
    tools: ["open_document", "get_document_symbols", "get_document_highlights"]
  - type: presence
    tools: ["open_document", "get_document_symbols", "get_document_highlights"]
  - type: absence
    tools: ["apply_edit", "rename_symbol", "get_references"]
