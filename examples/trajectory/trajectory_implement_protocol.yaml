# Trajectory assertion: validates the /lsp-implement skill protocol.
# get_server_capabilities must be called before go_to_implementation (capability gate).
# go_to_symbol + open_document must precede go_to_implementation.
# type_hierarchy follows go_to_implementation for subtype cross-referencing.
# Read-only — apply_edit and rename_symbol must NOT appear.
name: lsp-implement follows skill protocol
trace:
  - tool: get_server_capabilities
    args: {}
  - tool: go_to_symbol
    args:
      symbol_path: "main.Item"
  - tool: open_document
    args:
      file_path: "fixtures/go/main.go"
  - tool: go_to_implementation
    args:
      file_path: "fixtures/go/main.go"
      line: 6
      column: 6
  - tool: type_hierarchy
    args:
      file_path: "fixtures/go/main.go"
      line: 6
      column: 6
      direction: "subtypes"
trajectory:
  - type: order
    tools: ["get_server_capabilities", "go_to_symbol", "open_document", "go_to_implementation"]
  - type: presence
    tools: ["get_server_capabilities", "go_to_symbol", "go_to_implementation"]
  - type: absence
    tools: ["apply_edit", "rename_symbol"]
