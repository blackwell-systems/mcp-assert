name: fetch rejects invalid URL
server:
  command: /Users/dayna.blackwell/.local/bin/uvx
  args: [mcp-server-fetch]
assert:
  tool: fetch
  args:
    url: "not-a-url"
  expect:
    is_error: true
timeout: 30s
