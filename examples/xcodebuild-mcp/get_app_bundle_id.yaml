name: get_app_bundle_id returns isError without project
server:
  command: npx
  args: [-y, xcodebuildmcp@latest, mcp]
assert:
  tool: get_app_bundle_id
  args: {}
  expect:
    is_error: true
timeout: 30s
