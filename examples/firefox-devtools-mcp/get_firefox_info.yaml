name: get_firefox_info returns isError without Firefox running
server:
  command: npx
  args: [-y, firefox-devtools-mcp]
assert:
  tool: get_firefox_info
  args: {}
  expect:
    is_error: true
timeout: 15s
