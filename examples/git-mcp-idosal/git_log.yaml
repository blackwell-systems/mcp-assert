name: git_log returns commit history
server:
  command: npx
  args: [-y, git-mcp]
assert:
  tool: git_log
  args:
    cwd: "{{fixture}}"
  expect:
    not_error: true
    not_empty: true
timeout: 15s
