name: git_blame shows file annotations
server:
  command: npx
  args: [-y, git-mcp]
assert:
  tool: git_blame
  args:
    cwd: "{{fixture}}"
    file: test.txt
  expect:
    not_error: true
    not_empty: true
timeout: 15s
