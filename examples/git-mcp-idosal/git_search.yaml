name: git_search searches commit history
server:
  command: npx
  args: [-y, git-mcp]
assert:
  tool: git_search
  args:
    cwd: "{{fixture}}"
    term: "initial"
  expect:
    not_error: true
timeout: 15s
