name: git_status returns repo status
server:
  command: /Users/dayna.blackwell/.brewprune/bin/npx
  args: [-y, git-mcp]
assert:
  tool: git_status
  args:
    cwd: "{{fixture}}"
  expect:
    not_error: true
    not_empty: true
timeout: 15s
