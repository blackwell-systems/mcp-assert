name: git_status returns repo status
server:
  command: npx
  args: [-y, git-mcp]
assert:
  tool: git_status
  args:
    cwd: "{{fixture}}"
  expect:
    not_error: true
    not_empty: true
timeout: 15s
