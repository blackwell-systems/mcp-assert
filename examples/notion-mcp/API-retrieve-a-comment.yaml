name: API-retrieve-a-comment returns expected result
server:
    command: npx
    args: [-y, '@notionhq/notion-mcp-server']
assert:
    tool: API-retrieve-a-comment
    args:
        block_id: TODO
    expect:
        not_error: true
timeout: 30s
