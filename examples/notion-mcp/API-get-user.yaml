name: API-get-user returns expected result
server:
    command: npx
    args: [-y, '@notionhq/notion-mcp-server']
assert:
    tool: API-get-user
    args:
        user_id: TODO
    expect:
        not_error: true
timeout: 30s
