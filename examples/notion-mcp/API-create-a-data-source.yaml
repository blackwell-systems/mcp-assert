name: API-create-a-data-source returns expected result
server:
    command: npx
    args: [-y, '@notionhq/notion-mcp-server']
assert:
    tool: API-create-a-data-source
    args:
        parent: TODO
        properties: {}
    expect:
        not_error: true
timeout: 30s
