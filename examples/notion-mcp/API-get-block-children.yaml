name: API-get-block-children returns expected result
server:
    command: npx
    args: [-y, '@notionhq/notion-mcp-server']
assert:
    tool: API-get-block-children
    args:
        block_id: TODO
    expect:
        not_error: true
timeout: 30s
