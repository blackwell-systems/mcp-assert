name: API-update-a-data-source returns expected result
server:
    command: npx
    args: [-y, '@notionhq/notion-mcp-server']
assert:
    tool: API-update-a-data-source
    args:
        data_source_id: TODO
    expect:
        not_error: true
timeout: 30s
