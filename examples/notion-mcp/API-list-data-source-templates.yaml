name: API-list-data-source-templates returns expected result
server:
    command: npx
    args: [-y, '@notionhq/notion-mcp-server']
assert:
    tool: API-list-data-source-templates
    args:
        data_source_id: TODO
    expect:
        not_error: true
timeout: 30s
