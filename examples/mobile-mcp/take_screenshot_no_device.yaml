name: mobile_take_screenshot returns isError without connected device
server:
  command: npx
  args: [-y, "@mobilenext/mobile-mcp@latest"]
assert:
  tool: mobile_take_screenshot
  args: {}
  expect:
    is_error: true
timeout: 15s
