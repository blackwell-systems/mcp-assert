name: mobile_list_elements returns isError without device
server:
  command: npx
  args: [-y, "@mobilenext/mobile-mcp@latest"]
assert:
  tool: mobile_list_elements_on_screen
  args: {}
  expect:
    is_error: true
timeout: 15s
