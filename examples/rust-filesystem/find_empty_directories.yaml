name: find_empty_directories returns empty dirs
server:
  command: rust-mcp-filesystem
  args: ["{{fixture}}"]
assert:
  tool: find_empty_directories
  args:
    path: "{{fixture}}"
  expect:
    not_error: true
timeout: 30s
