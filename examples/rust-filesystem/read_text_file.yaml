name: read_text_file returns file contents
server:
  command: rust-mcp-filesystem
  args: ["{{fixture}}"]
assert:
  tool: read_text_file
  args:
    path: "{{fixture}}/hello.txt"
  expect:
    not_error: true
    contains: ["Hello, world!"]
timeout: 30s
