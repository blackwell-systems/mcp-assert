name: tail_file returns end of file
server:
  command: rust-mcp-filesystem
  args: ["{{fixture}}"]
assert:
  tool: tail_file
  args:
    path: "{{fixture}}/hello.txt"
    lines: 1
  expect:
    not_error: true
    contains: ["Hello, world!"]
timeout: 30s
