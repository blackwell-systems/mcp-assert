name: search_files finds files by pattern
server:
  command: rust-mcp-filesystem
  args: ["{{fixture}}"]
assert:
  tool: search_files
  args:
    path: "{{fixture}}"
    pattern: "*.txt"
  expect:
    not_error: true
    contains: ["hello.txt"]
timeout: 30s
