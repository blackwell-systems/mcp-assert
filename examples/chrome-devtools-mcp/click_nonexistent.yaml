name: click returns isError for nonexistent selector
server:
  command: npx
  args: [-y, chrome-devtools-mcp]
assert:
  tool: click
  args:
    selector: "#nonexistent"
  expect:
    is_error: true
timeout: 15s
