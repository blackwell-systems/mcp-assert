name: tavily_search handles empty query gracefully
server:
  command: npx
  args:
    - -y
    - tavily-mcp@latest
  env:
    TAVILY_API_KEY: ${TAVILY_API_KEY}
assert:
  tool: tavily_search
  args:
    query: ""
  expect:
    is_error: true
skip_unless_env: TAVILY_API_KEY
timeout: 30s
