name: kubectl_get_by_name returns isError for nonexistent resource
server:
  command: uvx
  args: [mcp-server-kubernetes]
assert:
  tool: kubectl_get_by_name
  args:
    kind: "pod"
    name: "nonexistent-pod"
    namespace: "default"
  expect:
    is_error: true
timeout: 15s
