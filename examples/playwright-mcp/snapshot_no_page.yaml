# Snapshot with no page navigated: should either return empty or error gracefully.
name: browser_snapshot handles no page loaded
server:
  command: /Users/dayna.blackwell/.brewprune/bin/npx
  args: [-y, "@playwright/mcp", --headless]
assert:
  tool: browser_snapshot
  args: {}
  expect:
    not_error: true
timeout: 30s
