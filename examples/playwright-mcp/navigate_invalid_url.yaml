name: browser_navigate rejects invalid URL
server:
  command: npx
  args: [-y, "@playwright/mcp", --headless]
assert:
  tool: browser_navigate
  args:
    url: "not-a-url"
  expect:
    is_error: true
timeout: 30s
