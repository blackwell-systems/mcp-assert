name: browser_network_requests returns without error
server:
  command: npx
  args: [-y, "@playwright/mcp", --headless]
setup:
  - tool: browser_navigate
    args:
      url: "https://example.com"
assert:
  tool: browser_network_requests
  args: {}
  expect:
    not_error: true
timeout: 30s
