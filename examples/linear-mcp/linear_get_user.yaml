name: linear_get_user returns expected result
server:
    command: npx
    args: [-y, mcp-server-linear]
assert:
    tool: linear_get_user
    args: {}
    expect:
        not_error: true
timeout: 30s
