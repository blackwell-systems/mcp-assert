name: linear_create_issues returns expected result
server:
    command: npx
    args: [-y, mcp-server-linear]
assert:
    tool: linear_create_issues
    args:
        issues: []
    expect:
        not_error: true
timeout: 30s
