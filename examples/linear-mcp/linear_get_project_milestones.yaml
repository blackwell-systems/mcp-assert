name: linear_get_project_milestones returns expected result
server:
    command: npx
    args: [-y, mcp-server-linear]
assert:
    tool: linear_get_project_milestones
    args:
        projectId: TODO
    expect:
        not_error: true
timeout: 30s
