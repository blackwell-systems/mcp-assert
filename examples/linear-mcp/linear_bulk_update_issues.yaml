name: linear_bulk_update_issues returns expected result
server:
    command: npx
    args: [-y, mcp-server-linear]
assert:
    tool: linear_bulk_update_issues
    args:
        issueIds: []
        update: {}
    expect:
        not_error: true
timeout: 30s
