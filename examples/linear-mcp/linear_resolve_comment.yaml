name: linear_resolve_comment returns expected result
server:
    command: npx
    args: [-y, mcp-server-linear]
assert:
    tool: linear_resolve_comment
    args:
        id: TODO
    expect:
        not_error: true
timeout: 30s
