name: linear_update_project_milestone returns expected result
server:
    command: npx
    args: [-y, mcp-server-linear]
assert:
    tool: linear_update_project_milestone
    args:
        id: TODO
    expect:
        not_error: true
timeout: 30s
