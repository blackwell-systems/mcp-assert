name: linear_edit_issue returns expected result
server:
    command: npx
    args: [-y, mcp-server-linear]
assert:
    tool: linear_edit_issue
    args:
        issueId: TODO
    expect:
        not_error: true
timeout: 30s
