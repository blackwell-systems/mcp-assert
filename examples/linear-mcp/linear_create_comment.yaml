name: linear_create_comment returns expected result
server:
    command: npx
    args: [-y, mcp-server-linear]
assert:
    tool: linear_create_comment
    args:
        body: TODO
        issueId: TODO
    expect:
        not_error: true
timeout: 30s
