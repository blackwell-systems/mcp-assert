name: linear_auth_callback returns expected result
server:
    command: npx
    args: [-y, mcp-server-linear]
assert:
    tool: linear_auth_callback
    args:
        code: TODO
    expect:
        not_error: true
timeout: 30s
