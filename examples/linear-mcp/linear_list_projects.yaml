name: linear_list_projects returns expected result
server:
    command: npx
    args: [-y, mcp-server-linear]
assert:
    tool: linear_list_projects
    args: {}
    expect:
        not_error: true
timeout: 30s
