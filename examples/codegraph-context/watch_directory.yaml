name: watch_directory returns expected result
server:
    command: uvx
    args: [codegraphcontext, mcp, start]
assert:
    tool: watch_directory
    args:
        path: /path/to/TODO
    expect:
        not_error: true
timeout: 30s
