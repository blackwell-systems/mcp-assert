name: check_job_status returns expected result
server:
    command: uvx
    args: [codegraphcontext, mcp, start]
assert:
    tool: check_job_status
    args:
        job_id: TODO
    expect:
        not_error: true
timeout: 30s
