name: discover_codegraph_contexts returns expected result
server:
    command: uvx
    args: [codegraphcontext, mcp, start]
assert:
    tool: discover_codegraph_contexts
    args: {}
    expect:
        not_error: true
timeout: 30s
