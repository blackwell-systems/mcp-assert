name: search_registry_bundles returns expected result
server:
    command: uvx
    args: [codegraphcontext, mcp, start]
assert:
    tool: search_registry_bundles
    args: {}
    expect:
        not_error: true
timeout: 30s
