name: list_watched_paths returns expected result
server:
    command: uvx
    args: [codegraphcontext, mcp, start]
assert:
    tool: list_watched_paths
    args: {}
    expect:
        not_error: true
timeout: 30s
