name: list_indexed_repositories returns expected result
server:
    command: uvx
    args: [codegraphcontext, mcp, start]
assert:
    tool: list_indexed_repositories
    args: {}
    expect:
        not_error: true
timeout: 30s
