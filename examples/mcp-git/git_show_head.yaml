name: git_show returns HEAD commit details
server:
  command: uvx
  args: [mcp-server-git]
assert:
  tool: git_show
  args:
    repo_path: "{{fixture}}"
    revision: HEAD
  expect:
    not_error: true
    not_empty: true
timeout: 30s
