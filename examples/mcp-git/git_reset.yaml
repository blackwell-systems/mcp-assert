name: git_reset resets to HEAD
server:
  command: /Users/dayna.blackwell/.local/bin/uvx
  args: [mcp-server-git]
assert:
  tool: git_reset
  args:
    repo_path: "{{fixture}}"
  expect:
    not_error: true
timeout: 15s
