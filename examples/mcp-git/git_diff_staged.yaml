name: git_diff_staged on clean repo
server:
  command: /Users/dayna.blackwell/.local/bin/uvx
  args: [mcp-server-git]
assert:
  tool: git_diff_staged
  args:
    repo_path: "{{fixture}}"
  expect:
    not_error: true
timeout: 15s
