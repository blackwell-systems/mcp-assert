name: git_create_branch creates new branch
server:
  command: /Users/dayna.blackwell/.local/bin/uvx
  args: [mcp-server-git]
assert:
  tool: git_create_branch
  args:
    repo_path: "{{fixture}}"
    branch_name: "mcp-assert-test"
  expect:
    not_error: true
timeout: 15s
