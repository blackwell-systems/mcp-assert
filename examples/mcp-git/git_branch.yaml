name: git_branch lists branches
server:
  command: uvx
  args: [mcp-server-git]
assert:
  tool: git_branch
  args:
    repo_path: "{{fixture}}"
    branch_type: "local"
  expect:
    not_error: true
    not_empty: true
    contains_any: ["main", "master"]
timeout: 30s
