name: git_diff_unstaged on clean repo
server:
  command: uvx
  args: [mcp-server-git]
assert:
  tool: git_diff_unstaged
  args:
    repo_path: "{{fixture}}"
  expect:
    not_error: true
timeout: 15s
