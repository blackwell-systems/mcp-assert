name: list_pull_requests returns PRs for mcp-assert
server:
  command: github-mcp-server
  args: [stdio]
assert:
  tool: list_pull_requests
  args:
    owner: blackwell-systems
    repo: mcp-assert
    state: all
    perPage: 5
  expect:
    not_error: true
timeout: 30s
