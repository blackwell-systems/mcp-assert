name: get_latest_release returns the latest mcp-assert release
server:
  command: github-mcp-server
  args: [stdio]
assert:
  tool: get_latest_release
  args:
    owner: blackwell-systems
    repo: mcp-assert
  expect:
    not_error: true
    not_empty: true
    contains: ["tag_name"]
timeout: 30s
