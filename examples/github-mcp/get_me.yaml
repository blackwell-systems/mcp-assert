name: get_me returns authenticated user info
server:
  command: github-mcp-server
  args: [stdio]
assert:
  tool: get_me
  args: {}
  expect:
    not_error: true
    not_empty: true
    contains: ["login"]
timeout: 30s
