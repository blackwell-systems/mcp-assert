name: search_repositories finds mcp-assert by name
server:
  command: github-mcp-server
  args: [stdio]
assert:
  tool: search_repositories
  args:
    query: "blackwell-systems/mcp-assert"
  expect:
    not_error: true
    not_empty: true
    contains: ["mcp-assert"]
timeout: 30s
