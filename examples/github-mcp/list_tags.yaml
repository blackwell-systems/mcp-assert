name: list_tags returns tags for mcp-assert
server:
  command: github-mcp-server
  args: [stdio]
assert:
  tool: list_tags
  args:
    owner: blackwell-systems
    repo: mcp-assert
  expect:
    not_error: true
    not_empty: true
    contains: ["v0.2.4"]
timeout: 30s
