name: list_buckets requires project ID
server:
  command: npx
  args: [-y, "@google-cloud/storage-mcp"]
assert:
  tool: list_buckets
  args: {}
  expect:
    is_error: true
    contains: ["Project ID not specified"]
timeout: 15s
