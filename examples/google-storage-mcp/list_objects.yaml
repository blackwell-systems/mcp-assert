name: list_objects handles missing bucket gracefully
server:
  command: npx
  args: [-y, "@google-cloud/storage-mcp"]
assert:
  tool: list_objects
  args:
    bucket_name: "nonexistent-bucket-99999"
  expect:
    not_error: true
timeout: 15s
