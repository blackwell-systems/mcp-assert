name: search_dashboards returns results with live backend
server:
  command: mcp-grafana
  args: [-t, stdio]
  env:
    GRAFANA_URL: "${GRAFANA_URL}"
    GRAFANA_SERVICE_ACCOUNT_TOKEN: "${GRAFANA_SERVICE_ACCOUNT_TOKEN}"
assert:
  tool: search_dashboards
  args: {}
  expect:
    not_error: true
timeout: 15s
