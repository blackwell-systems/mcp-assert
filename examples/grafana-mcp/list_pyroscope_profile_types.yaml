name: list_pyroscope_profile_types returns isError without Pyroscope
skip_unless_env: GRAFANA_SERVICE_ACCOUNT_TOKEN
server:
  command: mcp-grafana
  args: [-t, stdio]
assert:
  tool: list_pyroscope_profile_types
  args:
    datasource_uid: nonexistent
  expect:
    is_error: true
timeout: 15s
