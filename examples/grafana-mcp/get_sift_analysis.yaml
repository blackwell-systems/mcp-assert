name: get_sift_analysis returns isError without Sift
skip_unless_env: GRAFANA_SERVICE_ACCOUNT_TOKEN
server:
  command: mcp-grafana
  args: [-t, stdio]
assert:
  tool: get_sift_analysis
  args:
    investigation_id: nonexistent
  expect:
    is_error: true
timeout: 15s
