name: list_prometheus_metric_names returns metric names
skip_unless_env: GRAFANA_SERVICE_ACCOUNT_TOKEN
server:
  command: mcp-grafana
  args: [-t, stdio]
assert:
  tool: list_prometheus_metric_names
  args:
    datasource_uid: prometheus
  expect:
    is_error: true
timeout: 15s
