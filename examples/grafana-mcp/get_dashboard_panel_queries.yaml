name: get_dashboard_panel_queries returns isError for nonexistent dashboard
skip_unless_env: GRAFANA_SERVICE_ACCOUNT_TOKEN
server:
  command: mcp-grafana
  args: [-t, stdio]
assert:
  tool: get_dashboard_panel_queries
  args:
    uid: nonexistent
  expect:
    is_error: true
timeout: 15s
