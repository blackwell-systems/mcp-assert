name: create_incident creates and returns incident
skip_unless_env: GRAFANA_SERVICE_ACCOUNT_TOKEN
server:
  command: mcp-grafana
  args: [-t, stdio]
assert:
  tool: create_incident
  args:
    title: mcp-assert-test
  expect:
    not_error: true
timeout: 15s
