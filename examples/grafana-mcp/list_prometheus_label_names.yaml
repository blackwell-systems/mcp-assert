name: list_prometheus_label_names returns labels
skip_unless_env: GRAFANA_SERVICE_ACCOUNT_TOKEN
server:
  command: mcp-grafana
  args: [-t, stdio]
assert:
  tool: list_prometheus_label_names
  args:
    datasource_uid: prometheus
  expect:
    is_error: true
timeout: 15s
