name: get_alert_group returns isError for nonexistent group
skip_unless_env: GRAFANA_SERVICE_ACCOUNT_TOKEN
server:
  command: mcp-grafana
  args: [-t, stdio]
assert:
  tool: get_alert_group
  args:
    group_id: nonexistent
  expect:
    is_error: true
timeout: 15s
