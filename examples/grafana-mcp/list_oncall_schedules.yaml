name: list_oncall_schedules returns isError without OnCall
skip_unless_env: GRAFANA_SERVICE_ACCOUNT_TOKEN
server:
  command: mcp-grafana
  args: [-t, stdio]
assert:
  tool: list_oncall_schedules
  args:
    {}
  expect:
    is_error: true
timeout: 15s
