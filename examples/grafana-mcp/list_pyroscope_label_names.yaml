name: list_pyroscope_label_names returns isError without Pyroscope
skip_unless_env: GRAFANA_SERVICE_ACCOUNT_TOKEN
server:
  command: mcp-grafana
  args: [-t, stdio]
assert:
  tool: list_pyroscope_label_names
  args:
    datasource_uid: nonexistent
  expect:
    is_error: true
timeout: 15s
