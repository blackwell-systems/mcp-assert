name: query_prometheus_histogram returns isError without expr
skip_unless_env: GRAFANA_SERVICE_ACCOUNT_TOKEN
server:
  command: mcp-grafana
  args: [-t, stdio]
assert:
  tool: query_prometheus_histogram
  args:
    datasource_uid: prometheus
  expect:
    is_error: true
timeout: 15s
