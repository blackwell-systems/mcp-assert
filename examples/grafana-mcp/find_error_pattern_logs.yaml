name: find_error_pattern_logs returns isError without datasource
skip_unless_env: GRAFANA_SERVICE_ACCOUNT_TOKEN
server:
  command: mcp-grafana
  args: [-t, stdio]
assert:
  tool: find_error_pattern_logs
  args:
    datasource_uid: nonexistent
  expect:
    is_error: true
timeout: 15s
