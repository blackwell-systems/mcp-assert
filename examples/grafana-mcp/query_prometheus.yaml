name: query_prometheus returns isError without datasource
skip_unless_env: GRAFANA_SERVICE_ACCOUNT_TOKEN
server:
  command: mcp-grafana
  args: [-t, stdio]
assert:
  tool: query_prometheus
  args:
    expr: up
  expect:
    is_error: true
timeout: 15s
