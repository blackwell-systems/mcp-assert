name: get_dashboard_by_uid handles missing dashboard gracefully
server:
  command: mcp-grafana
  args: [-t, stdio]
assert:
  tool: get_dashboard_by_uid
  args:
    uid: "nonexistent-uid-12345"
  expect:
    not_error: true
timeout: 15s
