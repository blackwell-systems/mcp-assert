name: get_annotations returns annotations list
skip_unless_env: GRAFANA_SERVICE_ACCOUNT_TOKEN
server:
  command: mcp-grafana
  args: [-t, stdio]
assert:
  tool: get_annotations
  args:
    {}
  expect:
    not_error: true
timeout: 15s
