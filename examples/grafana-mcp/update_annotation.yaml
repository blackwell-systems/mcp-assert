name: update_annotation returns isError for nonexistent annotation
skip_unless_env: GRAFANA_SERVICE_ACCOUNT_TOKEN
server:
  command: mcp-grafana
  args: [-t, stdio]
assert:
  tool: update_annotation
  args:
    annotation_id: 99999
  expect:
    is_error: true
timeout: 15s
