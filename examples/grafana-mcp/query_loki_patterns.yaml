name: query_loki_patterns returns isError without query
skip_unless_env: GRAFANA_SERVICE_ACCOUNT_TOKEN
server:
  command: mcp-grafana
  args: [-t, stdio]
assert:
  tool: query_loki_patterns
  args:
    datasource_uid: loki
  expect:
    is_error: true
timeout: 15s
