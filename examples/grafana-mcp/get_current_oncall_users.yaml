name: get_current_oncall_users returns isError without OnCall
skip_unless_env: GRAFANA_SERVICE_ACCOUNT_TOKEN
server:
  command: mcp-grafana
  args: [-t, stdio]
assert:
  tool: get_current_oncall_users
  args:
    {}
  expect:
    is_error: true
timeout: 15s
