name: list_incidents returns incident list
skip_unless_env: GRAFANA_SERVICE_ACCOUNT_TOKEN
server:
  command: mcp-grafana
  args: [-t, stdio]
assert:
  tool: list_incidents
  args:
    {}
  expect:
    not_error: true
timeout: 15s
