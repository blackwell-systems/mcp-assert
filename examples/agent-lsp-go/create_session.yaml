name: create_simulation_session returns a session ID
server:
  command: agent-lsp
  args: ["go:gopls"]
setup:
  - tool: start_lsp
    args:
      root_dir: "{{fixture}}"
  - tool: open_document
    args:
      file_path: "{{fixture}}/main.go"
      language_id: go
assert:
  tool: create_simulation_session
  args:
    workspace_root: "{{fixture}}"
    language: go
  expect:
    not_error: true
    not_empty: true
    contains: ["session"]
timeout: 120s
