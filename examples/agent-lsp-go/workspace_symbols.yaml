name: find_symbol finds Person type
server:
  command: agent-lsp
  args: ["go:gopls"]
setup:
  - tool: start_lsp
    args:
      root_dir: "{{fixture}}"
  - tool: open_document
    args:
      file_path: "{{fixture}}/main.go"
      language_id: go
assert:
  tool: find_symbol
  args:
    query: "Greet"
  expect:
    not_error: true
    not_empty: true
    contains: ["Greet"]
timeout: 30s
