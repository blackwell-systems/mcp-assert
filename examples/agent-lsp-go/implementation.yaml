name: go_to_implementation on Person struct
server:
  command: agent-lsp
  args: ["go:gopls"]
setup:
  - tool: start_lsp
    args:
      root_dir: "{{fixture}}"
  - tool: open_document
    args:
      file_path: "{{fixture}}/main.go"
      language_id: go
assert:
  tool: go_to_implementation
  args:
    file_path: "{{fixture}}/main.go"
    line: 6
    column: 6
  expect:
    not_error: true
timeout: 30s
