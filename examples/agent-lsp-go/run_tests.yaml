name: run_tests executes Go test suite
server:
  command: agent-lsp
  args: ["go:gopls"]
setup:
  - tool: start_lsp
    args:
      root_dir: "{{fixture}}"
assert:
  tool: run_tests
  args:
    workspace_dir: "{{fixture}}"
  expect:
    not_error: true
timeout: 120s
