name: run_build compiles the Go fixture
server:
  command: agent-lsp
  args: ["go:gopls"]
setup:
  - tool: start_lsp
    args:
      root_dir: "{{fixture}}"
assert:
  tool: run_build
  args:
    workspace_dir: "{{fixture}}"
  expect:
    not_error: true
timeout: 120s
