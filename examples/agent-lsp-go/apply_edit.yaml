name: apply_edit adds a comment via text-match mode
server:
  command: agent-lsp
  args: ["go:gopls"]
setup:
  - tool: start_lsp
    args:
      root_dir: "{{fixture}}"
  - tool: open_document
    args:
      file_path: "{{fixture}}/main.go"
      language_id: go
assert:
  tool: apply_edit
  args:
    file_path: "{{fixture}}/apply_edit_fixture.go"
    old_text: "// apply_edit_fixture: dedicated target for apply_edit assertions."
    new_text: "// apply_edit_fixture: dedicated target for apply_edit assertions.\n// Updated by mcp-assert test."
  expect:
    not_error: true
timeout: 120s
