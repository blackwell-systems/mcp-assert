name: get_tests_for_file returns test files for main.go
server:
  command: agent-lsp
  args: ["go:gopls"]
setup:
  - tool: start_lsp
    args:
      root_dir: "{{fixture}}"
  - tool: open_document
    args:
      file_path: "{{fixture}}/main.go"
      language_id: go
assert:
  tool: get_tests_for_file
  args:
    file_path: "{{fixture}}/main.go"
  expect:
    not_error: true
timeout: 30s
