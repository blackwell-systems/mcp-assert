name: get_cross_repo_references rejects empty consumer_roots
server:
  command: agent-lsp
  args: ["go:gopls"]
setup:
  - tool: start_lsp
    args:
      root_dir: "{{fixture}}"
  - tool: open_document
    args:
      file_path: "{{fixture}}/main.go"
      language_id: go
  - tool: get_diagnostics
    args:
      file_path: "{{fixture}}/main.go"
assert:
  tool: get_cross_repo_references
  args:
    symbol_file: "{{fixture}}/main.go"
    line: 7
    column: 6
    consumer_roots: []
  expect:
    is_error: true
    contains: ["consumer_roots"]
timeout: 120s
