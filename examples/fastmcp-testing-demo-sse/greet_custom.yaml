name: greet with custom greeting (SSE)
server:
  transport: sse
  url: http://127.0.0.1:8082/sse
assert:
  tool: greet
  args:
    name: Bob
    greeting: Howdy
  expect:
    not_error: true
    not_empty: true
    contains:
    - Howdy
    - Bob
timeout: 30s
