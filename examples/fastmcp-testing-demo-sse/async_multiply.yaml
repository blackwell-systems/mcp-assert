name: async_multiply returns the product (SSE)
server:
  transport: sse
  url: http://127.0.0.1:8082/sse
assert:
  tool: async_multiply
  args:
    x: 6.0
    y: 7.0
  expect:
    not_error: true
    not_empty: true
    contains:
    - '42'
timeout: 30s
