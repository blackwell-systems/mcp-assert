name: get-tiny-image returns expected result
server:
    command: npx
    args: [-y, '@modelcontextprotocol/server-everything']
assert:
    tool: get-tiny-image
    args: {}
    expect:
        not_error: true
timeout: 30s
