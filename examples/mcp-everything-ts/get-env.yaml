name: get-env returns expected result
server:
    command: npx
    args: [-y, '@modelcontextprotocol/server-everything']
assert:
    tool: get-env
    args: {}
    expect:
        not_error: true
timeout: 30s
