name: get-resource-links returns expected result
server:
    command: npx
    args: [-y, '@modelcontextprotocol/server-everything']
assert:
    tool: get-resource-links
    args: {}
    expect:
        not_error: true
timeout: 30s
