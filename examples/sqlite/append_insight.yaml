name: append_insight adds a business insight
server:
  command: /Users/dayna.blackwell/.local/bin/uvx
  args: [mcp-server-sqlite, --db-path, "{{fixture}}/test.db"]
assert:
  tool: append_insight
  args:
    insight: "Test insight from mcp-assert"
  expect:
    not_error: true
timeout: 15s
